name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  checks:
    strategy:
      matrix:
        os: ['ubuntu-22.04', 'macos-12']

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1

      - name: Runs checks
        run: echo "Done!"

